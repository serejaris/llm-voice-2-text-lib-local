# Video Processing Configuration

# Path to FFmpeg binary (optional, will use system PATH if not specified)
# For MacBook M1/M2/M3 installed via Homebrew:
# FFMPEG_PATH=/opt/homebrew/bin/ffmpeg
# For Intel Mac:
# FFMPEG_PATH=/usr/local/bin/ffmpeg

# Maximum video processing time in milliseconds (default: 300000 = 5 minutes)
# Increase for longer videos
# VIDEO_PROCESSING_TIMEOUT=600000

# Whether to keep original video files after extraction (default: false)
# KEEP_ORIGINAL_VIDEO=false

# Format for extracted audio (default: wav, optimized for Whisper)
# EXTRACTED_AUDIO_FORMAT=wav

# Whisper Configuration

# Whisper model to use (default: large-v3)
# Options: tiny, base, small, medium, large-v3, large-v3-turbo
# Note: large-v3 provides excellent Russian language support
# WHISPER_MODEL=large-v3
